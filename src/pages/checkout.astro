---
import { buildTenantData } from "@lib/buildTenantData";

import CheckoutPage from "@checkout/CheckoutPage.astro";

const { tenant, data } = await buildTenantData("checkout");

---

<CheckoutPage {...data} tenant={tenant} />
