---
import { buildTenantData } from "@lib/buildTenantData";

import ReservaPage from "@template/ReservaPage.astro";

const { tenant, data } = await buildTenantData("pedido");

// if (!tenant.pages.reserva) return Astro.redirect("/")
return Astro.redirect("/")

---


