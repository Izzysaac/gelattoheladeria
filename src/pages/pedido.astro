---
import { buildTenantData } from "@lib/buildTenantData";

import PedidoPage from "@template/PedidoPage.astro";

const { tenant, data } = await buildTenantData("menu");

if (!tenant.pages.pedido) return Astro.redirect("/");
---

<PedidoPage {...data} tenant={tenant}/>
