---
import { buildTenantData } from "@lib/buildTenantData";

import TicketPage from "@ticket/TicketPage.astro";

const { tenant, data } = await buildTenantData("ticket");

if (!tenant.pages.ticket) return Astro.redirect("/");
---

<TicketPage {...data} tenant={tenant}/>
