---
import { buildTenantData } from "@lib/buildTenantData";

import MenuPage from "@pedido/MenuPage.astro";

const { tenant, data } = await buildTenantData("menu");

if (!tenant.pages.menu) return Astro.redirect("/");
---

<MenuPage {...data} tenant={tenant} />
