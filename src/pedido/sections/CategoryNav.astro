---
const { categorias } = Astro.props;

// 🔹 Grupos hardcodeados
const GRUPOS = {
    bebidas: [
        'Ensaladas Gelattos',
        'Copas de Helado',
        'Otras Delicias',
        'Bebidas'
    ],
    comidas: [
        'Hamburgesas',
        'Patacones',
        'Salchipapas',
        'Hot Dog',
        'Sandwiches',
        'Picadas',
        'Menú Infantil'
    ]
};

// 🔹 Grupo activo (default)
const activeGroup = "bebidas";

// 🔹 Filtrado
const categoriasFiltradas = categorias.filter(cat =>
    GRUPOS[activeGroup].includes(cat)
);

---

<div id="menuGroupsContainer" class="sticky top-0 z-10 bg-(--text-brand)">
<!-- 🔘 Botones de grupo -->
    <div class="menu-groups flex items-center justify-center text-lg" >
        <button data-group="bebidas" class="group-btn active">
            Helados y Bebidas
        </button>
        <button data-group="comidas" class="group-btn">
            Comidas Rápidas
        </button>
    </div>
<!-- 📌 NavMenu -->
    <nav id="navMenu" class="category-nav max-w-200 sm:top-0!" aria-label="Categorías del menú">
        <ul class="m-0 flex gap-0">
            {
                categoriasFiltradas.map((categoria: string) => (
                    <li>
                        <button
                        data-category={categoria}
                        data-scroll
                        >
                            {categoria}
                        </button>
                        <div class="linea" />
                    </li>
                ))
            }
        </ul>
    </nav>
</div>

<!-- <nav id="navMenu" class="category-nav max-w-200 sm:top-0!" aria-label="Categorías del menú">
    <ul class="m-0 flex gap-0">
        {
            categorias.map((categoria: string) => (
                <li class="">
                    <button
                        data-category={categoria}
                        data-scroll
                    >
                        {categoria}
                    </button>
                    <div class="linea" />
                </li>
            ))
        }
    </ul>
</nav> -->

<style is:global>
    .category-nav {
        width: 100vw;
    }

    .category-nav ul {
        overflow: scroll;
        scroll-snap-type: x mandatory;
        scroll-behavior: smooth;
        padding: 0rem 0rem 0.25rem 0rem;
        list-style: none;
    }
    .category-nav ul::-webkit-scrollbar {
        display: none;
    }

    .category-nav button {
        display: block;
        padding: 0.5rem 0.75rem;
        white-space: nowrap;
        text-decoration: none;
        color: var(--neutral-10);
    }

    .category-nav button:focus-visible {
        outline: 2px solid black;
        outline-offset: 2px;
    }
    .category-nav button.active {
        font-weight: 700;
        color: var(--color-navmenu-active);
    }

    .linea {
        position: relative;
        height: 3px;
        background-color: var(--neutral-20);
    }

    .linea-activa:after {
        content: "";
        position: absolute;
        left: 0;
        right: 0;
        top: 0;
        height: 3px;
        background-color: var(--color-navmenu-active);
        border-radius: 999px;
    }

    .active {
        font-weight: 700;
    }

    /* .group-btn {
        padding: 6px 12px;
        border: 2px solid var(--text-brand);
        border-radius: 9999px;
        background: none;
        cursor: pointer;
        font-size: 1rem;
    } */

    .menu-groups {
        padding: 12px 4px 0px 4px;
        background-color: var(--text-brand);
        color: white;
    }

    .group-btn {
        padding: 8px 0px;
        width: 100%;
        border: 2px solid var(--text-brand);
        border-radius: 9999px;
        background: none;
        cursor: pointer;
    }
    .group-btn.active {
        background-color: white;
        color: var(--text-brand);
    }
</style>

<script src="src/scripts/menu/newNavigation.js"></script>
