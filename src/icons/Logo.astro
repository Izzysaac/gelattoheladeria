---
const { titulo, logo } = Astro.props;
---

<a href="/" class="w-full h-full flex justify-center">
    <img
        id="logo"
        src={logo}
        alt={titulo}
        class="rounded-full max-w-25 pointer-events-none"
        width="30%"
        fetchpriority="high"
        loading="eager"
    />
</a>
